{
  "name": "hospital-food-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready MVP for Hospital Food Management System (HFMS)",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "setup": "npm install && npm run db:push -w apps/api && npm run db:seed -w apps/api",
    "dev": "concurrently -n API,WEB -c blue,green \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "start": "npm run start -w apps/api",
    "build": "npm run build -w apps/web",
    "smoke:test": "npm run smoke:test -w apps/api"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
